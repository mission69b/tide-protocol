[package]
name = "faith_router"
edition = "2024"
version = "0.0.1"
# Update this after publishing
published-at = "0x0"

[dependencies]
# tide_core inherits its address from its own Move.toml (v3 testnet)
tide_core = { local = "../../core" }

[addresses]
faith_router = "0x0"
